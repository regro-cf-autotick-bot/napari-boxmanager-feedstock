{% set name = "napari-boxmanager" %}
{% set version = "0.4.7" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/napari_boxmanager-{{ version }}.tar.gz
  sha256: 3cf6a691af030736d40b1fee240efb7a11b6b7c5f8a4d183331d116f0c08a940

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - pip
    - python >=3.10
    - setuptools-scm
  run:
    - matplotlib-base
    - mrcfile
    - napari >=0.4.17
    - numpy <=1.23.5  # https://github.com/napari/napari/issues/5484, should be fixed in the next napari release
    - pandas
    - pystardb >=0.4.2
    - python >=3.10
    - qtpy
    - scipy
    - tifffile
    - tqdm

test:
  imports:
    - box_manager
  commands:
    - pip list
    - pip check
  requires:
    - pip
    - pyqt

about:
  home: https://pypi.org/project/napari-boxmanager
  summary: Particle selection tool for cryo-em
  license: MPL-2.0
  license_file: LICENSE
  dev_url: https://github.com/MPI-Dortmund/napari-boxmanager

extra:
  recipe-maintainers:
    - goanpeca
    - jaimergp
    - mstabrin
